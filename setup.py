from setuptools import setup

setup(
    name='mrs_connectivity_perch',
    version='0.0.1',
    packages=['mrs_connectivity_perch'],
    install_requires=[
        'gymnasium',
        'numpy',
        'matplotlib',
    ],
)
